# Esempio MDX

Questo è un esempio di documentazione scritta in **MDX**.

<Info>
Puoi usare componenti React direttamente nei tuoi file MDX!
</Info>

## Installazione

Per installare il pacchetto:

```bash
npm install my-package
```

## Utilizzo

Ecco un esempio di codice TypeScript:

```typescript
import { useState } from "react";

const Counter = () => {
  const [count, setCount] = useState(0);
  
  return (
    <button onClick={() => setCount(c => c + 1)}>
      Contatore: {count}
    </button>
  );
};

export default Counter;
```

<Warning>
Assicurati di configurare correttamente le dipendenze prima di usare il pacchetto.
</Warning>

## Funzionalità

- ✅ Supporto TypeScript nativo
- ✅ Componenti React integrati
- ✅ Syntax highlighting automatico
- ✅ Tabelle e blocchi citazione

### Tabella di esempio

| Feature | Supportato |
|---------|------------|
| MDX | ✅ |
| React | ✅ |
| TypeScript | ✅ |

<Success>
Ora sei pronto per creare documentazione con MDX!
</Success>
